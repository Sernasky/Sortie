<div class="container my-3 mx-3 w-auto">
<nav class="navbar navbar-expand-lg navbar-light bg-success">
    <div class="container-fluid">
        <h1><a class="navbar-brand" href="{{ path('main_home') }}">
                <img src="{{ asset('img/logo_sortir.png') }}" alt="Logo sortir" style="width: 110px">
            </a>
            Sortir.com
        </h1>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                <a class="nav-link active" aria-current="page" href="{{ path('sorties_list') }}">Sorties</a>
                {% if app.user %}
                    <a class="nav-link" href="{{ path('user', {id : app.user.id}) }}">Mon profil</a>
                    <a class="nav-link" href="{{ path('app_logout') }}">Déconnexion ({{ app.user.pseudo }})</a>
                {% if is_granted("ROLE_ADMIN") %}
                    <a class="nav-link" href="{{ path('admin_villes') }}">Villes</a>
                    <a class="nav-link" href="{{ path('admin_campus') }}">Campus</a>
                {% endif %}
                {% else %}
                    <a class="nav-link" href="{{ path('app_login') }}">Se connecter</a>
                    <a class="nav-link" href="{{ path('app_register') }}">S'inscrire</a>
                {% endif %}
            </div>
        </div>
    </div>
</nav></div>
