<nav>
    <ul>
        <li><a href="{{ path('sorties_list') }}" title="Sorties">Sorties</a></li>

        {% if app.user %}
            <li><a href="{{ path('user', {id : app.user.id}) }}" title="Modifier mon profil">Mon profil</a></li>
            <li><a href="{{ path('app_logout') }}" title="Logout">Déconnexion({{ app.user.pseudo }})</a></li>

            {% if is_granted("ROLE_ADMIN") %}
                <li><a href="{{ path('admin_villes') }}" title="Villes">Villes</a></li>
                <li><a href="{{ path('admin_campus') }}" title="Campus">Campus</a></li>
            {% endif %}

        {% else %}
            <li><a href="{{ path('app_login') }}" title="Login">Se connecter</a></li>
            <li><a href="{{ path('app_register') }}" title="Create account">S'inscrire</a></li>
        {% endif %}
    </ul>
</nav>
